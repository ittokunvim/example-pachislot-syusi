<% flash.each do |key, value| %>
  <div id="flash_<%= key %>" data-controller="shared" data-target="shared.notice">
    <%= content_tag(:span, value) %>
    <button data-action="click->shared#delete">x</button>
  </div>
<% end %>
