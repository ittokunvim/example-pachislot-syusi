<%# REQUIRE: @blance %>

<% content_for(:title, t(".title", date: l(@blance.date))) %>
<% provide(:button_text, t(".button_text")) %>

<%= render "form" %>

<%= link_to t(".delete"), @blance, data: { turbo_method: :delete, turbo_confirm: t(".confirm") } %>
